<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>{{site_title}}</title>

  <!-- Bootstrap CSS -->
  <link
    href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
    rel="stylesheet"
  />

  <style>
    html, body { height: 100%; margin: 0; overflow: hidden; } header, footer {
    position: fixed; width: 100%; z-index: 1030; } header { top: 0; } footer {
    bottom: 0; } main { margin-top: 80px; margin-bottom: 40px; overflow-y: auto;
    height: calc(100% - 120px); padding: 1rem; } .banner { display: flex;
    align-items: center; background-color: var(--bs-dark); color: white;
    padding: 1rem; } .vertical-nav { display: flex; flex-direction: column;
    margin-right: 1rem; } .site-title { flex-grow: 1; text-align: center; }
    .auth-block { display: flex; flex-direction: column; align-items: flex-end;
    gap: 0.5rem; } .logo { height: 60px; width: auto; } .theme-toggle-btn {
    background: none; border: 1px solid white; color: white; border-radius:
    0.25rem; padding: 0.25rem 0.5rem; font-size: 0.875rem; }
  </style>
</head>